<nav class="navbar navbar-inverse">
  <%= link_to 'Your Storage', root_path, id: 'logo' %>
  <nav>
    <ul class="nav navbar-nav pull-right">
      <% if logged_in? %>
          <li class="btn-grd"><%= link_to 'My Drive', current_user%></li>
        <li class="dropdown btn-grd">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" >
            Account<b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li class="btn-grd"><%= link_to 'Settings', '#' %></li>
            <li class="divider"></li>
            <li class="btn-grd"><%= link_to 'Log out', logout_path, method: 'delete' %> </li>
          </ul>
        </li>
      <% else %>
          <li class="btn-grd"><%= link_to 'Log in', login_path %> </li>
      <% end %>
    </ul>
  </nav>
</nav>